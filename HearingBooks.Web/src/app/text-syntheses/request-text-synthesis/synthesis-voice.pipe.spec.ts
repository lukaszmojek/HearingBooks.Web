import { SynthesisVoicePipe } from './synthesis-voice.pipe';

describe('SynthesisVoicePipe', () => {
  it('create an instance', () => {
    const pipe = new SynthesisVoicePipe();
    expect(pipe).toBeTruthy();
  });
});
