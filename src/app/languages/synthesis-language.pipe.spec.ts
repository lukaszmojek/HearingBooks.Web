import { SynthesisLanguagePipe } from './synthesis-language.pipe';

describe('SynthesisLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new SynthesisLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
