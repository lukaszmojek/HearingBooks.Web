import { DialogueSynthesisPipe } from './dialogue-synthesis.pipe';

describe('DialogueSynthesisPipe', () => {
  it('create an instance', () => {
    const pipe = new DialogueSynthesisPipe();
    expect(pipe).toBeTruthy();
  });
});
